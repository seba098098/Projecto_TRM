# prefect.yaml
deployments:
  - name: "prediccion-bitcoin-deployment"
    entrypoint: "Work_Flow/Work_Flow.py:flujo_prediccion_bitcoin"  # Ruta relativa al flow
    storage:
      type: git
      repository: "https://github.com/seba098098/Projecto_TRM.git"
      reference: "main"             # Rama
      folder: "Work_Flow"           # Subcarpeta (opcional si el entrypoint ya incluye la ruta)
      timezone: "UTC"